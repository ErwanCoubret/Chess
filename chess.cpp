#include <string>
#include <vector>
#include <iostream>
#include "utils.h"

using namespace std;

int main() {
    // printBegin();

    // int gameStart = getInputStart();
    int gameStart = 1;

    while (gameStart)
    {
        Board chessBoard;
        chessBoard.init();
        chessBoard.begin();

        break;
    }

    printQuit();
    return 0;
}